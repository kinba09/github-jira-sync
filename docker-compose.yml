services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: github-jira-sync-backend
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
